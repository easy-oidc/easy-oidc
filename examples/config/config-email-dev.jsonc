// Easy OIDC <https://easy-oidc.dev>
// Copyright The Easy OIDC Authors
// SPDX-License-Identifier: Apache-2.0

{
  "$schema": "https://easy-oidc.dev/schema/v2/config.schema.json",

  // Local email authentication with Mailpit. See DEV.md for setup instructions.
  "issuer_url": "http://localhost:8080",
  "http_listen_addr": "127.0.0.1:8080",
  "signing_algorithm": "RS256",

  "secrets": {
    "provider": "env",
    "signing_key_name": "EASYOIDC_SIGNING_KEY",
  },

  "state_database": { "driver": "sqlite", "path": "temp/easy-oidc-state.db" },

  "user_login_connectors": {
    "email": {
      "type": "email",
      "display_name": "Email code",
    },
  },

  "email": {
    "verification_mode": "disabled",
    "otp_secret_name": "EASYOIDC_OTP_SECRET",
    "otp_ttl": "5m",
    "smtp": {
      "host": "localhost",
      "port": 1025,
      "tls_mode": "plaintext",
      "from_name": "Easy OIDC",
      "from_address": "auth@localhost",
    },
  },

  "static_policy": {
    "require_user_groups_from_policy": false,
    "default_redirect_uris": ["http://localhost:8000"],
    "clients": {
      "kubelogin-local": {},
    },
  },
}
